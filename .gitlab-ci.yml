image: alpine:latest

stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  script:
    - echo "build ok"

test-job:
  stage: test
  script:
    - mvn test
  artifacts:
    when: always
    paths:
      - target/surefire-reports/*.xml
    reports:
      junit: target/surefire-reports/*.xml

deploy-job:
  stage: deploy
  script:
    - echo "Simulação de deploy na branch main"
  only:
    - main
